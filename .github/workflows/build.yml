name: Build (Windows)

on:
  workflow_dispatch:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3

      - name: Update submodules
        run: git submodule update --init --recursive
        shell: bash

      - name: Install vcpkg
        run: |
          git clone https://github.com/microsoft/vcpkg.git C:\vcpkg
          C:\vcpkg\bootstrap-vcpkg.bat

      - name: Install dependencies
        run: |
          C:\vcpkg\vcpkg install sdl2 libogg libvorbis libtheora glew portaudio

      - name: Install make
        run: choco install make -y

      - name: Build
        run: make
        shell: cmd
